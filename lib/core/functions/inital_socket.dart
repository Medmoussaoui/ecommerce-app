
/*

import 'package:socket_io_client/socket_io_client.dart' as IO;

IO.Socket initialSocket(String url, {bool autoConnect = false}) {
  IO.Socket so = IO.io(url, {
    'autoConnect': autoConnect,
    'transports': ['websocket'],
  });
  return so;
}


*/